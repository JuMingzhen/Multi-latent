# Multi-latent Reasoning Project - Unified Requirements
# 整合了 data/scripts, train, eval 三个模块的所有依赖

# ============================================================================
# 核心依赖（所有模块都需要）
# ============================================================================
torch>=2.0.0
transformers>=4.30.0
numpy>=1.21.0

# ============================================================================
# 训练模块依赖 (train/)
# ============================================================================
accelerate>=0.20.0
datasets>=2.0.0

# ============================================================================
# 评测模块依赖 (eval/)
# ============================================================================
openai>=1.0.0

# ============================================================================
# 数据生成和可视化依赖 (data/scripts/)
# ============================================================================
networkx>=2.6
matplotlib>=3.5

# ============================================================================
# 可选依赖（用于开发和调试）
# ============================================================================
# tensorboard>=2.10.0  # 用于训练可视化（可选）
# jupyter>=1.0.0       # 用于notebook（可选）
# ipython>=8.0.0       # 用于交互式开发（可选）

